#### 1. InteliJ를 켠다(없으면 설치)
#### 2. J-AI_SERBOT_PROJECT(프로젝트-폴더)를 연다 (Open -> J-AI-SERBOT_PROJECT)
#### 3. backend-server(서버) -> Main.java를 먼저 실행시킨다.
#### 4. desktop-client(GUI) -> Main.java 를 실행시킨다.
    4.1 현재 GUI는 wasd를 누르면 로봇이 움직이는 상태이다. 2025.12.04일 기준
    4.2 물론 주피터(로봇)서버도 켜야 실행된다.
#### 5. 주피터(로봇 wlan 아이피주소:8888 에 들어간다.
#### 6. 현재는 서버연결예시.ipybn 파일에 들어가면 robot-ws/ex_server.py 파일을 실행할 수 있다.
    6.1 vscode에서 ssh를 통해 접속을 하려면 18.5v로 접속해야해서 주피터로 작업하는게 편할 수 있음.
    6.2 본인이 vscode 버전 18.5(2023년버전)으로 다운그레이드 후 작업해도 상관없음
#### 7. 로봇 작업자는 본인이 작업하고 그 파일을 robot-ws 폴더 안에 새로 py파일을 만들어서 저장해줘야함(백업용)
